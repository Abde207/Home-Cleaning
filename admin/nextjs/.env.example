# Server-only Admin settings. Copy to ignored .env.local for standalone local development.
APP_ENVIRONMENT=development
BACKEND_API_URL=http://127.0.0.1:3001/api/v1
# Never use NEXT_PUBLIC_ for backend credentials or private endpoints.
