'use client';

import type { AdminCompanyDetail, AdminExtra, AdminService, AdminTeamDetail, TeamAvailability, TeamMember } from '../lib/admin-api';
import type { Locale } from '../lib/i18n';
import { messagesFor } from '../messages';
import { ActionForm, Field, Heading, Select, Toggle } from './provider-catalog';

function date(value: string | null, locale: Locale) { return value ? new Intl.DateTimeFormat(locale === 'ar' ? 'ar-JO' : 'en-JO', { timeZone: 'Asia/Amman', dateStyle: 'medium', timeStyle: 'short' }).format(new Date(value)) : '—'; }
function status(value: string) { return value.replaceAll('_', ' '); }

export function CompanyDetailManagementView({ locale, row }: { locale: Locale; row: AdminCompanyDetail }) {
  const t = messagesFor(locale);
  return <div className="dashboard-stack"><Heading phase="14D" title={row.name} description={`${row.internalCode} · ${status(row.status)}`} back={`/${locale}/providers/companies`} backLabel={t.backToList} />
    <section className="detail-grid"><article className="panel"><h2>{t.companySummary}</h2><dl className="detail-list"><dt>{t.internalCode}</dt><dd>{row.internalCode}</dd><dt>{t.status}</dt><dd>{status(row.status)}</dd><dt>{t.commission}</dt><dd>{row.commissionRate}</dd><dt>{t.created}</dt><dd>{date(row.createdAt, locale)}</dd></dl><ActionForm path={`admin/companies/${row.id}`} method="PUT" locale={locale} submit={t.save}><div className="form-grid"><Field label={t.internalCode} name="internalCode" defaultValue={row.internalCode} /><Field label={t.name} name="name" defaultValue={row.name} /><Select label={t.status} name="status" defaultValue={row.status} options={['PENDING', 'ACTIVE', 'SUSPENDED', 'INACTIVE']} /><Field label={t.commission} name="commissionRate" defaultValue={row.commissionRate} /></div></ActionForm></article><article className="panel"><h2>{t.operationalCounts}</h2><dl className="detail-list"><dt>{t.teams}</dt><dd>{row.counts.teams}</dd><dt>{t.activeTeams}</dt><dd>{row.counts.activeTeams}</dd><dt>{t.activeManagers}</dt><dd>{row.counts.activeManagers}</dd><dt>{t.openAssignments}</dt><dd>{row.counts.openAssignments}</dd><dt>{t.unsettledPayables}</dt><dd>{row.counts.unsettledPayables}</dd></dl></article></section>
    <section className="panel"><h2>{t.serviceAreas}</h2>{row.serviceAreas.length ? <div className="table-wrap"><table><thead><tr><th>{t.name}</th><th>{t.coordinates}</th><th>{t.radius}</th><th>{t.actions}</th></tr></thead><tbody>{row.serviceAreas.map(area => <tr key={area.id}><td>{area.name}</td><td>{area.latitude}, {area.longitude}</td><td>{area.radiusKm} km</td><td><ActionForm path={`admin/companies/${row.id}/service-areas/${area.id}`} method="PUT" locale={locale} submit={t.save}><div className="form-grid"><Field label={t.name} name="name" defaultValue={area.name} /><Field label={t.latitude} name="latitude" defaultValue={area.latitude} number /><Field label={t.longitude} name="longitude" defaultValue={area.longitude} number /><Field label={t.radius} name="radiusKm" defaultValue={area.radiusKm} /><Toggle label={t.active} name="active" defaultChecked={area.active} /></div></ActionForm></td></tr>)}</tbody></table></div> : <p className="empty-state">{t.emptyState}</p>}<h3>{t.addServiceArea}</h3><ActionForm path={`admin/companies/${row.id}/service-areas`} locale={locale} submit={t.add}><div className="form-grid"><Field label={t.name} name="name" /><Field label={t.latitude} name="latitude" number /><Field label={t.longitude} name="longitude" number /><Field label={t.radius} name="radiusKm" defaultValue="5" /><Toggle label={t.active} name="active" defaultChecked /></div></ActionForm></section>
  </div>;
}

export function TeamDetailManagementView({ locale, row, members, availability, capabilities, services }: { locale: Locale; row: AdminTeamDetail; members: TeamMember[]; availability: TeamAvailability[]; capabilities: string[]; services: AdminService[] }) {
  const t = messagesFor(locale);
  return <div className="dashboard-stack"><Heading phase="14D" title={row.name} description={`${row.company.name} · ${row.internalCode}`} back={`/${locale}/providers/teams`} backLabel={t.backToList} /><section className="detail-grid"><article className="panel"><h2>{t.teamSummary}</h2><dl className="detail-list"><dt>{t.company}</dt><dd>{row.company.name}</dd><dt>{t.status}</dt><dd>{status(row.status)}</dd><dt>{t.capacity}</dt><dd>{row.capacity}</dd><dt>{t.location}</dt><dd>{row.latitude !== null ? `${row.latitude}, ${row.longitude}` : t.neverReported}</dd><dt>{t.locationAt}</dt><dd>{date(row.locationAt, locale)}</dd></dl><ActionForm path={`provider/teams/${row.id}`} method="PUT" locale={locale} submit={t.save}><div className="form-grid"><Field label={t.name} name="name" defaultValue={row.name} /><Field label={t.capacity} name="capacity" defaultValue={row.capacity} number /><Toggle label={t.active} name="active" defaultChecked={row.active} /></div></ActionForm><ActionForm path={`provider/teams/${row.id}/availability-status`} method="PUT" locale={locale} submit={t.saveStatus}><Select label={t.status} name="status" defaultValue={row.status} options={['AVAILABLE', 'BUSY', 'OFFLINE', 'PAUSED']} /></ActionForm></article><article className="panel"><h2>{t.capabilities}</h2><ActionForm path={`provider/teams/${row.id}/capabilities`} method="PUT" locale={locale} submit={t.saveCapabilities}><fieldset><legend>{t.services}</legend>{services.map(service => <label className="check-field" key={service.id}><input name="serviceIds" type="checkbox" value={service.id} defaultChecked={capabilities.includes(service.id)} />{locale === 'ar' ? service.nameAr : service.name}</label>)}</fieldset></ActionForm></article></section><section className="panel"><h2>{t.members}</h2>{members.length ? <div className="table-wrap"><table><thead><tr><th>{t.name}</th><th>{t.role}</th></tr></thead><tbody>{members.map(member => <tr key={member.id}><td>{member.name}</td><td>{member.role}</td></tr>)}</tbody></table></div> : <p className="empty-state">{t.emptyState}</p>}<p className="read-only-note">{t.memberProvisioningNote}</p><ActionForm path="admin/users" locale={locale} submit={t.provision}><div className="form-grid"><Field label={t.phone} name="phone" /><Field label={t.name} name="name" /></div><input type="hidden" name="role" value="TEAM_LEADER_CLEANER" /><input type="hidden" name="companyId" value={row.companyId} /><input type="hidden" name="teamId" value={row.id} /></ActionForm></section><section className="panel"><h2>{t.schedule}</h2>{availability.length ? <div className="table-wrap"><table><thead><tr><th>{t.starts}</th><th>{t.ends}</th><th>{t.available}</th><th>{t.actions}</th></tr></thead><tbody>{availability.map(item => <tr key={item.id}><td colSpan={2}><ActionForm path={`provider/teams/${row.id}/availability/${item.id}`} method="PUT" locale={locale} submit={t.save}><div className="form-grid"><Field label={t.starts} name="startsAt" type="datetime-local" defaultValue={item.startsAt.slice(0, 16)} /><Field label={t.ends} name="endsAt" type="datetime-local" defaultValue={item.endsAt.slice(0, 16)} /><Toggle label={t.available} name="available" defaultChecked={item.available} /></div></ActionForm></td><td>{item.available ? t.available : t.unavailable}</td><td><ActionForm path={`provider/teams/${row.id}/availability/${item.id}`} method="DELETE" locale={locale} submit={t.remove}><span /></ActionForm></td></tr>)}</tbody></table></div> : <p className="empty-state">{t.emptyState}</p>}<ActionForm path={`provider/teams/${row.id}/availability`} locale={locale} submit={t.add}><div className="form-grid"><Field label={t.starts} name="startsAt" type="datetime-local" /><Field label={t.ends} name="endsAt" type="datetime-local" /><Toggle label={t.available} name="available" defaultChecked /></div></ActionForm></section></div>;
}

export function ServiceDetailManagementView({ locale, row, extras }: { locale: Locale; row: AdminService; extras: AdminExtra[] }) {
  const t = messagesFor(locale);
  return <div className="dashboard-stack"><Heading phase="14D" title={row.name} description={row.code} back={`/${locale}/catalog/services`} backLabel={t.backToList} /><section className="panel"><h2>{t.serviceSummary}</h2><ActionForm path={`admin/services/${row.id}`} method="PUT" locale={locale} submit={t.save}><div className="form-grid"><Field label={t.code} name="code" defaultValue={row.code} /><Field label={t.name} name="name" defaultValue={row.name} /><Field label={t.nameAr} name="nameAr" defaultValue={row.nameAr} /><Field label={t.basePrice} name="basePrice" defaultValue={row.basePrice} /><Field label={t.duration} name="durationMinutes" defaultValue={row.durationMinutes} number /></div><label>{t.description}<textarea name="description" defaultValue={row.description ?? ''} /></label><Toggle label={t.active} name="active" defaultChecked={row.active} /></ActionForm></section><section className="panel"><h2>{t.extras}</h2>{extras.length ? <div className="table-wrap"><table><thead><tr><th>{t.name}</th><th>{t.nameAr}</th><th>{t.price}</th><th>{t.status}</th></tr></thead><tbody>{extras.map(extra => <tr key={extra.id}><td colSpan={2}><ActionForm path={`admin/services/${row.id}/extras/${extra.id}`} method="PUT" locale={locale} submit={t.save}><div className="form-grid"><Field label={t.code} name="code" defaultValue={extra.code} /><Field label={t.name} name="name" defaultValue={extra.name} /><Field label={t.nameAr} name="nameAr" defaultValue={extra.nameAr} /><Field label={t.price} name="price" defaultValue={extra.price} /><Toggle label={t.active} name="active" defaultChecked={extra.active} /></div></ActionForm></td><td>{extra.price}</td><td>{extra.active ? t.active : t.inactive}</td></tr>)}</tbody></table></div> : <p className="empty-state">{t.emptyState}</p>}<h3>{t.addExtra}</h3><ActionForm path={`admin/services/${row.id}/extras`} locale={locale} submit={t.add}><div className="form-grid"><Field label={t.code} name="code" /><Field label={t.name} name="name" /><Field label={t.nameAr} name="nameAr" /><Field label={t.price} name="price" /></div><Toggle label={t.active} name="active" /></ActionForm></section></div>;
}
